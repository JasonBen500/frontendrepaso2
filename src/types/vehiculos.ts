export interface Vehiculos {
  idVehiculo: number | null;
  placa: string;
  marca: string;
  modelo: string;
  color: string;
  precioDia: number;
}
export interface ApiMensaje {
  mensaje: string;
}
